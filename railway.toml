[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter server db:generate && pnpm build:server"

[deploy]
startCommand = "cd apps/server && npx prisma migrate deploy && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[environments.production.variables]
NODE_ENV = "production"